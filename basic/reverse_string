'''name = input("Enter a string: ")

for i in range(len(name) - 1, -1, -1):
    print(name[i], end="") '''
'''x = input("enter a name : ")
for i in range(len(x)):
    print(x[i], end="")'''

'''x = input("enter a name : ")
print(len(x))'''

'''x = "python"

for i in range(len(x)):
    print(i, x[i],end="")'''

name = input("Enter a string: ")

upper = 0
lower = 0

for ch in name:
    # uppercase check pannu
    # lowercase check pannu

    print("Uppercase =", upper)
    print("Lowercase =", lower)